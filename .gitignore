# Local integration symlinks; these are convenience links to actual submodules.
website/docs
/website/target/

# Local reference repositories; these are migration authorities, not submodules,
# except for foundation-base, which is registered with the workspace.
/reference/*
!/reference/foundation-base/

# macOS
.DS_Store

# Local Netlify folder
.netlify
