# Canonical video sources

Place master footage, screen captures, turntables, and animation exports here. Supported video formats are tracked through Git LFS by the root `.gitattributes` contract.

Keep delivery encodes and poster images in the repository or object store that publishes them, together with a small manifest linking back to the exact workspace source revision.
